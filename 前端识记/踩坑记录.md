1. `indexOf`无法检索到`NaN`是否重复，如果不是要查找下标，使用`includes`
2. ```js
     /**
   * 
   * @param {Array} arr 
   * @param {Function} fn 
   * @returns 
   */
  function myMap(arr, fn) {
    // TODO
    return arr.reduce((prev,cur,idx)=>{
      // 我提供规定函数被调用的参数，两个参数，当前元素、当前索引，并且要执行
      let res = fn(cur,idx);
      prev.push(res)
      return prev
    },[])
  }
  // 我提供需要执行的函数，请按照规则传入对应的参数，所以在ts中是预先规定参数意义；（也可以是我指定好，反正上下两个参数的意义需要对应）
  eq('3. myMap', myMap([1, 2, 3], (x, i) => x * 10 + i), [10, 21, 32]);


   ```
3. 