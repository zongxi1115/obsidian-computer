1. 不能一再强调设计模式，设计模式不是目的是手段，是为了吧经常变的地方，把要变动的逻辑减少，一部分固定起来这就是设计模式，（这跟函数一样，不能莫名其妙就新建一个函数，即使只调用一次）
所以优先考虑朴素实现。
2. 为了避免考虑+1问题，所有问题全部左闭右开`[l,r)`，长度就是`r-l`，空区间就是`l==r`，拼接`[a,b)+[b,c)=[a,c)`
要给每一个变量精确的语义。不要在脑中模拟
```
❌ "i 用来遍历数组" —— 这是动作描述,验证不了任何东西
❌ "i 大概在已处理和未处理的分界附近" —— 模糊,±1 说不清
✅ "每次循环开始时,[0, i) 的元素已写入结果,i 指向第一个未处理元素"
```


3. 